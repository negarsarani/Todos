import El from '../library/El';

const Mainperent = (child) => {
  return El({
    element: 'div',
    child,
  });
};
// if (3 > 2) {
//   console.log("s");
// }
export default Mainperent;

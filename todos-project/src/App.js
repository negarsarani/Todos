import Mainperent from './layout';
import El from './library/El';
const App = () => {
  const app = El({
    element: 'div',
    className: 'parent',
    child,
  });
  return Mainperent(app);
};
Mainperent;
if (3 > 2) {
  console.log('object');
}

export default App;
